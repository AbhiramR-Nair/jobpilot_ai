"""Application package for jobpilot_ai.

This package contains the modular components used by the assistant:

- `agents`: feature modules (job finding, parsing, embeddings, CV optimization, ATS checks, etc.)
- `graph`: LangGraph workflow and state
- `tools`: shared utilities (DOCX helpers, text cleaning, logging, OpenAI client)
- `database`: SQLite database layer
- `ui`: Streamlit dashboard

Most modules are currently stubs with documentation and type hints only.
"""

