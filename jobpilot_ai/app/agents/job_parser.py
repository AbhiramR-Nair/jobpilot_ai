"""Job description parsing module for `jobpilot_ai`.

Responsibilities (to be implemented later):

- Define data models for parsed job descriptions (title, company, location,
  responsibilities, requirements, benefits, compensation, etc.).
- Provide interfaces for LLM-based parsing of unstructured job text
  into the structured models.
- Support extraction of key skills, technologies, and experience levels
  for downstream matching and ATS-style checks.
- Integrate with the SQLite layer to store parsed job metadata and
  link it to the original job posting records.

This module currently contains only documentation and placeholder structure.
Parsing logic and model calls will be added in a later iteration.
"""

