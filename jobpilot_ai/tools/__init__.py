"""Shared utility subpackage for `jobpilot_ai`.

The `tools` package groups cross-cutting helpers that can be reused by
multiple feature modules without introducing tight coupling.

Planned utility modules (to be implemented later) include:

- Configuration loading and environment handling.
- Centralized logging configuration.
- Common data types, enums, and small helper functions.
- Basic error and exception types used across the project.

Only documentation and empty placeholders are defined at this stage.
"""

